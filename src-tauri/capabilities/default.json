{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for per-display overlay windows",
  "windows": ["overlay-*"],
  "permissions": [
    "core:default"
  ]
}
